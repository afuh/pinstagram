mixin formImage()
  form.form__image.col(action="" method="POST" enctype="multipart/form-data")
    input(type="file" name="photo" id="photo" accept="image/png, image/jpeg" required)
    textarea(name="caption" placeholder="Write a caption...")
    input.button(type="submit" value="Share")
