mixin userBox(data = {})
  .box__content
      ul.contact-list
        each user in data
          li.row
            a.img(href=`/${user.slug}`)
              img(src=h.avatar(user), alt=`${user.name}'s avatar`)
            .user-name.col  
              a(href=`/${user.slug}`)= user.username
              span= user.name
