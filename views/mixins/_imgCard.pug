mixin imgCard(image = {})
  section
    .img-card
      .img-card__header.row
        a.img(href=`/${image.author.slug}`) 
          img(src=`${image.author.gravatar}&s=30 || "https://unsplash.it/30/30"}`, alt=`${image.author.name}'s' avatar`)
        span= image.author.username
      .img-card__image
        img(src=`/uploads/images/${image.photo}` alt=`${image.caption}`)  
      .img-card__content
        .content__icons
          if user
            span ♥️
            span 💬
        .content__likes
          span 15 Me gusta
        .content__comments
          p.caption= image.caption
          ul
            li
              a(href="#") usuario1 
              span Lorem ipsum dolor sit amet, consectetur elit
            li
              a(href="#") usuario2 
              span Lorem ipsum dolor sit amet, consectetur adipisicing elit  
        .img-card__date
          p= h.moment(image.created).startOf().fromNow()
      .img-card__comment
        if (user)
          form.comment(id="comment", method="POST", action="#")
            input(name="text", placeholder="Añade un comentario...")
