extends layout
include mixins/_imgCard

block content
  section
    - const path = like ? '/like' : ''
    .current-img.row
      .prev-img
        if prev
          a.arrow.prev.row(href=`/p/${prev}${path}`) ❮
      +imgCard(current)
      .next-img
        if next
          a.arrow.next.row(href=`/p/${next}${path}`) ❯  
